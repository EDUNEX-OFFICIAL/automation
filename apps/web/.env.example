NEXT_PUBLIC_API_URL=/api-upstream
NEXT_PUBLIC_DEV_WEB_PORT=3000
API_UPSTREAM_URL=http://127.0.0.1:4000

# Socket:Windows par `localhost` ::1 ko de sakta hai (alag process + 404) — IPv4 stable rahegi.
NEXT_PUBLIC_SOCKET_URL=http://127.0.0.1:4000
