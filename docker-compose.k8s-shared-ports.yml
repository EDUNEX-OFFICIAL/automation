# Host-only Postgres/Redis for automation (pgAdmin via SSH tunnel).
# k8s / WAN blocked by deploy/scripts/hostinger/secure-vps.sh on the VPS.
services:
  postgres:
    ports:
      - '127.0.0.1:32459:5432'

  redis:
    ports:
      - '127.0.0.1:6380:6379'
